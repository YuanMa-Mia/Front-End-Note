# JavaScript 继承方式

> 参考：《JavaScript 高级程序设计》（红宝书）第八章
> 适用：3年经验前端面试高频考点

---

## 六种继承方式

红宝书把继承的演进讲得很清晰，本质上是一个不断打补丁的过程，每种方式都是在解决上一种的缺陷。

---

### 1. 原型链继承

**为什么能继承？**

JavaScript 查找属性时，会先在对象自身找，找不到就去 `__proto__` 找，再找不到就继续沿着链往上，这就是原型链查找机制。原型链继承利用的正是这一点：把子类的 `prototype` 替换成父类的一个实例，那么子类实例在自身找不到某个属性时，就会去 `Dog.prototype`（也就是那个父类实例）上找，再找不到就去父类实例的 `__proto__`（也就是 `Animal.prototype`）上找，于是父类原型上的所有方法都变得可访问了。

```js
function Animal(name) {
  this.name = name
  this.colors = ['red', 'blue']
}
Animal.prototype.getName = function() {
  return this.name
}

function Dog() {}
Dog.prototype = new Animal('animal')  // 关键：替换原型
Dog.prototype.constructor = Dog

const d = new Dog()
```

执行 `Dog.prototype = new Animal('animal')` 之后，原型链变成这样：

```
d
└── __proto__ → Dog.prototype（Animal 的实例，上面有 name、colors）
                └── __proto__ → Animal.prototype（上面有 getName）
                                └── __proto__ → Object.prototype
                                                └── __proto__ → null
```

所以 `d.getName()` 能找到，是因为 JS 引擎沿着这条链一路找上去，在 `Animal.prototype` 上找到了这个方法。

**缺点：** `new Animal('animal')` 这个父类实例被挂在了 `Dog.prototype` 上，它上面的 `colors` 数组是所有 `Dog` 实例共享的同一个引用。`d1.colors.push('green')` 修改的是这个共享数组，`d2.colors` 自然也变了。另外子类实例化时没有机会向父类传参，因为父类只在 `new Animal('animal')` 那一次被调用。

---

### 2. 借用构造函数继承（经典继承）

**为什么能继承？**

`call` 的作用是改变函数执行时的 `this` 指向。`Animal.call(this, name)` 的意思是：把 `Animal` 这个函数在当前 `Dog` 实例的上下文里执行一遍，`Animal` 内部所有 `this.xxx = ...` 的赋值操作，都会落在这个 `Dog` 实例上。相当于把父类构造函数的初始化逻辑"复制"到了子类实例身上。

```js
function Animal(name) {
  this.name = name
  this.colors = ['red', 'blue']
}

function Dog(name, breed) {
  Animal.call(this, name)  // Animal 在 Dog 实例的 this 上跑了一遍
  this.breed = breed
}

const d1 = new Dog('旺财', '柴犬')
const d2 = new Dog('小黑', '拉布拉多')
```

`new Dog()` 执行时，JS 先创建一个空对象作为 `this`，然后进入 `Dog` 函数体，`Animal.call(this, name)` 让 `Animal` 在这个空对象上跑，于是 `this.name = name`、`this.colors = ['red', 'blue']` 都写到了这个新对象上。每次 `new Dog()` 都会重新执行这段逻辑，所以每个实例都有自己独立的 `colors` 数组，互不干扰。

**缺点：** `Animal.call(this)` 只是借用了构造函数里的代码，完全没有碰 `Animal.prototype`，所以 `Animal.prototype.getName` 对 `Dog` 实例来说是不可见的。方法只能定义在构造函数体内，每次 `new Dog()` 都会重新创建这些函数对象，无法共享，内存浪费。

---

### 3. 组合继承（最常用的 ES5 方案）

**为什么能继承？**

把前两种的优点合并：用 `Animal.call(this)` 保证每个实例有自己独立的属性副本，用 `Dog.prototype = new Animal()` 建立原型链保证方法可以复用。两条路各司其职，互不干扰。

```js
function Animal(name) {
  this.name = name
  this.colors = ['red', 'blue']
}
Animal.prototype.getName = function() {
  return this.name
}

function Dog(name, breed) {
  Animal.call(this, name)   // ① 实例属性：每个实例独立
  this.breed = breed
}
Dog.prototype = new Animal()  // ② 原型方法：所有实例共享
Dog.prototype.constructor = Dog

const d1 = new Dog('旺财', '柴犬')
```

`d1` 自身有 `name`、`colors`、`breed`（来自 ①），原型链上有 `getName`（来自 ②），两者都满足。

**缺点：** 父类构造函数被调用了两次。第一次是 `Dog.prototype = new Animal()`，这次调用会在 `Dog.prototype` 上写入 `name` 和 `colors`；第二次是 `new Dog()` 时执行 `Animal.call(this)`，又在实例上写入了 `name` 和 `colors`。实例自身的属性会遮蔽原型上的同名属性，所以 `Dog.prototype` 上那份 `name` 和 `colors` 永远用不到，纯属多余，白白占内存。

```
d1 自身：{ name: '旺财', colors: [...], breed: '柴犬' }
         ↑ 遮蔽了下面这层
Dog.prototype（Animal实例）：{ name: undefined, colors: [...] }  ← 多余的！
         ↓
Animal.prototype：{ getName }
```

---

### 4. 原型式继承

**为什么能继承？**

道格拉斯·克罗克福德的思路是：不一定非要用构造函数，只要能让新对象的 `__proto__` 指向目标对象，就能访问目标对象上的所有属性和方法。他用一个临时构造函数 `F` 作为桥梁，把 `F.prototype` 设为目标对象，然后 `new F()` 出来的实例，其 `__proto__` 自然就指向了目标对象。

```js
function object(o) {
  function F() {}   // 临时构造函数，只是个壳
  F.prototype = o   // 把目标对象挂到原型上
  return new F()    // 新对象的 __proto__ 就指向了 o
}

// ES5 将这个模式标准化为：
// Object.create(o)

const animal = {
  name: 'animal',
  colors: ['red', 'blue'],
  getName() { return this.name }
}

const dog = Object.create(animal)
dog.name = '旺财'
```

`new F()` 创建的对象，其 `__proto__` 指向 `F.prototype`，也就是 `o`。所以 `dog` 访问 `getName` 时，在自身找不到，就去 `__proto__`（也就是 `animal`）上找，找到了。本质上和原型链继承是同一个机制，只是不再需要构造函数，更轻量。

**缺点：** 和原型链继承一样，`animal.colors` 是引用类型，所有基于 `animal` 创建的对象共享同一个数组。这种方式适合的场景是"我只想要一个和某个对象相似的新对象"，而不是正式的类层次结构。

---

### 5. 寄生式继承

**为什么能继承？**

在原型式继承的基础上包一层工厂函数，创建完新对象之后，在返回之前往上面挂一些额外的方法，起到"增强"的效果。继承的原理和原型式完全一样，只是多了一个统一添加方法的入口。

```js
function createDog(original) {
  const clone = Object.create(original)  // 原型式继承，建立原型链

  // 增强：给新对象添加额外能力
  clone.bark = function() {
    console.log('汪汪！')
  }
  clone.run = function() {
    console.log('跑起来！')
  }

  return clone
}

const animal = { name: 'animal', colors: ['red', 'blue'] }
const dog = createDog(animal)
dog.bark()  // '汪汪！'
```

**缺点：** `bark`、`run` 这些方法是在工厂函数里直接赋值的，每次调用 `createDog` 都会创建全新的函数对象，不同实例之间的 `bark` 不是同一个函数引用，无法复用。这和借用构造函数的问题如出一辙。

---

### 6. 寄生组合式继承（ES5 终极方案）⭐

**为什么能继承，又为什么比组合继承更好？**

组合继承的问题根源在于：为了建立原型链，不得不执行一次 `new Animal()`，而这次执行会把父类的实例属性写到 `Dog.prototype` 上，造成浪费。

寄生组合式的核心洞察是：建立原型链，本质上只需要让 `Dog.prototype.__proto__` 指向 `Animal.prototype` 就够了，根本不需要跑父类构造函数。`Object.create(Animal.prototype)` 做的正是这件事——创建一个空对象，其 `__proto__` 指向 `Animal.prototype`，不执行任何构造函数逻辑，干净利落。

```js
function inheritPrototype(Child, Parent) {
  // 创建一个 __proto__ 指向 Parent.prototype 的空对象
  // 不调用 Parent 构造函数，所以不会产生多余的实例属性
  const proto = Object.create(Parent.prototype)
  proto.constructor = Child   // 修正 constructor 指向
  Child.prototype = proto
}

function Animal(name) {
  this.name = name
  this.colors = ['red', 'blue']
}
Animal.prototype.getName = function() {
  return this.name
}

function Dog(name, breed) {
  Animal.call(this, name)  // 父类构造函数只调用这一次
  this.breed = breed
}

inheritPrototype(Dog, Animal)

const d1 = new Dog('旺财', '柴犬')
const d2 = new Dog('小黑', '拉布拉多')

d1.colors.push('green')
console.log(d2.colors)    // ['red', 'blue'] ✅
console.log(d1.getName()) // '旺财' ✅
```

执行完之后，原型链是这样的：

```
d1 自身：{ name: '旺财', colors: [...], breed: '柴犬' }
         ↓ __proto__
Dog.prototype（干净的空对象，只有 constructor）
         ↓ __proto__
Animal.prototype：{ getName }
         ↓ __proto__
Object.prototype
         ↓ __proto__
null
```

对比组合继承，`Dog.prototype` 上不再有多余的 `name` 和 `colors`，原型链干净，父类构造函数也只调用了一次。这就是为什么它是 ES5 的最优方案，也是 ES6 `class extends` 在引擎层面实际采用的机制。

---

## 对比总结

| 继承方式 | 引用类型独立 | 可传参 | 方法可复用 | 父类构造函数调用次数 |
|----------|:-----------:|:------:|:----------:|:-------------------:|
| 原型链继承 | ❌ | ❌ | ✅ | 1次 |
| 借用构造函数 | ✅ | ✅ | ❌ | 1次 |
| 组合继承 | ✅ | ✅ | ✅ | **2次** |
| 原型式继承 | ❌ | ❌ | ✅ | 0次 |
| 寄生式继承 | ❌ | ❌ | ❌ | 0次 |
| **寄生组合式** | ✅ | ✅ | ✅ | **1次** ⭐ |

---

## 演进脉络

六种方式的演进，本质上是在解决两个核心矛盾：

**实例属性独立** 需要在子类构造函数里用 `call` 借用父类构造函数，让每个实例自己初始化一份。

**方法复用** 需要建立原型链，让子类实例能沿着 `__proto__` 找到父类原型上的方法。

前五种方式要么只解决了其中一个，要么解决了两个但引入了新问题。寄生组合式用 `Object.create` 把"建立原型链"和"执行父类构造函数"这两件事彻底解耦，才做到了两全其美。

```
原型链继承（方法可复用，但引用类型共享）
  ↓ 问题：引用类型共享、无法传参
借用构造函数（实例属性独立，但方法不可复用）
  ↓ 问题：方法无法复用
组合继承（两者结合，但父类构造函数调用两次）
  ↓ 问题：Dog.prototype 上有多余的父类实例属性
寄生组合式（Object.create 解耦原型链建立与构造函数执行）⭐ ES5 终极方案
  ↓ ES6 语法糖
class extends（底层即寄生组合式）
```
