
# 事件循环（Event Loop）

> 参考：https://juejin.cn/post/6873424205791100942

---

## 面试怎么答

> 面试时建议按"背景 → 机制 → 细节 → 举例"四层展开，不要上来就背流程。


**第一层：背景（为什么需要 Event Loop）**

JS 是单线程语言，同一时间只能做一件事。但浏览器中有很多耗时操作，比如网络请求、定时器，如果同步等待这些操作完成，页面就会卡死。所以 JS 用异步 + 事件循环的方式来解决这个问题——耗时操作交给浏览器的其他线程去处理，处理完了再把回调放到队列里，等主线程空闲了再执行。

**第二层：核心机制（一句话版本，适合开场）**

> JS 是单线程的，Event Loop 是它处理异步任务的调度机制——同步代码执行完后，优先清空微任务队列，再取宏任务执行，每个宏任务执行完都会再次清空微任务，如此循环。

**第三层：关键细节（体现深度，等面试官追问再展开）**

1. **整段脚本本身就是第一个宏任务**，所以执行顺序是：脚本（宏任务）→ 清空微任务 → 下一个宏任务 → 清空微任务 → 循环
2. **微任务清空是"到底"的**，不是取一个，而是一直取到队列为空。如果微任务执行过程中又产生了新的微任务，会继续在本轮执行，不会等到下一轮
3. **async/await 属于微任务**，`await` 后面的代码等价于 `.then()` 的回调，这在做输出题时是关键

**第四层：举例（主动用代码收尾）**

概念说完后，主动说"我可以用一段代码来验证一下这个顺序"，写出 setTimeout + Promise 混合的例子，一步步推导输出结果，既展示理解深度，又证明能实际应用。

---

## 执行顺序

```
整段脚本（第一个宏任务）开始执行
  → 清空微任务队列（直到队列为空，新产生的微任务也在本轮清空）
  → 浏览器判断是否需要渲染（可选，不是每轮必走）
      ├── 需要 → 执行 rAF 回调 → 渲染（Layout → Paint → Composite）
      └── 不需要 → 跳过
  → 取下一个宏任务执行
  → 循环...
```

---

## 任务分类

| 类型 | 常见 API |
|------|----------|
| 宏任务 | `setTimeout`、`setInterval`、`MessageChannel`、I/O 回调 |
| 微任务 | `Promise.then/catch/finally`、`MutationObserver`、`queueMicrotask`、`async/await` 后续代码 |

> ⚠️ `setImmediate` 是 Node.js 专有 API，浏览器中不存在，面试时不要混淆。

---

## 核心规则

- 每个宏任务执行完后，必须**清空全部微任务**，才能进入下一步
- 微任务执行过程中新产生的微任务，会继续追加到队列尾部，**本轮一并清空**
- 渲染夹在"微任务清空"和"下一个宏任务"之间，但**不是每轮都渲染**，浏览器按需决定（约 16.6ms / 帧）

---

## 为什么渲染在微任务之后？

微任务是"当前任务的延续"，浏览器希望等所有微任务跑完再统一渲染，避免中间状态被用户看到。宏任务之间才允许插入渲染帧，保证页面不卡死。

---

## requestAnimationFrame

- rAF 回调在**渲染前**执行，是渲染流程的一部分，不是宏任务也不是微任务
- 节奏与屏幕刷新率对齐，适合做动画
- `setTimeout(fn, 16)` 做动画会有误差，rAF 不会

---

## 经典输出题

### 基础版（验证微任务优先于宏任务）

```javascript
console.log('1');

setTimeout(() => console.log('4'), 0);

Promise.resolve()
  .then(() => console.log('2'))
  .then(() => console.log('3'));

// 输出：1 → 2 → 3 → 4
```

`setTimeout(fn, 0)` 不是"立刻执行"，而是"下一轮事件循环"，要等微任务全部清空后才执行。

---

### 进阶版（宏任务内部产生微任务）

```javascript
console.log(1);

setTimeout(() => {
  console.log(2);
  Promise.resolve().then(() => console.log(3));
}, 0);

Promise.resolve()
  .then(() => {
    console.log(4);
    setTimeout(() => console.log(5), 0);
  })
  .then(() => console.log(6));

console.log(7);

// 输出：1 → 7 → 4 → 6 → 2 → 3 → 5
```

**推导步骤：**

1. 同步执行：输出 `1`、`7`，setTimeout① 进宏任务队列，Promise.then① 进微任务队列
2. 清空微任务：执行 then①，输出 `4`，setTimeout② 进宏任务队列；then① 完成触发 then②，输出 `6`
3. 取宏任务 setTimeout①：输出 `2`，then③ 进微任务队列；立即清空微任务，输出 `3`
4. 取宏任务 setTimeout②：输出 `5`

---

### async/await 版（高频考点）

```javascript
async function async1() {
  console.log('async1 start');       // 2
  await async2();
  console.log('async1 end');         // 6（微任务）
}

async function async2() {
  console.log('async2');             // 3
}

console.log('script start');         // 1

setTimeout(() => console.log('setTimeout'), 0);  // 8（宏任务）

async1();

new Promise(resolve => {
  console.log('promise1');           // 4
  resolve();
}).then(() => console.log('promise2'));           // 7（微任务）

console.log('script end');           // 5

// 输出：script start → async1 start → async2 → promise1 → script end
//       → async1 end → promise2 → setTimeout
```

**关键点：** `await async2()` 执行时，`async2` 函数体同步执行完（输出 `async2`），然后 `await` 把后续代码（`async1 end`）包装成微任务挂起，主线程继续往下走。

---

## 一句话总结

宏任务是**工作单元**，微任务是**工作单元的尾巴**，渲染是**工作单元之间的喘息机会**。
